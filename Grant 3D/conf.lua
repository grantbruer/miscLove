function love.conf(t)
    t.window.title = "NO"        -- The title of the window the game is in (string)
    t.version = "11.5"                  -- The LÖVE version this game was made for (string)

    t.console = false           -- Attach a console (boolean, Windows only)
end